{
    "scripts": {
        "solana:build:program": "cargo build-bpf --manifest-path=programs/solana-app/Cargo.toml --bpf-out-dir=dist/solana/program"
    },
    "dependencies": {
        "@project-serum/anchor": "^0.19.0"
    },
    "devDependencies": {
        "chai": "^4.3.4",
        "mocha": "^9.1.3"
    }
}
